# Sinh nội dung Lớp Nền không cần API key — agent Claude Code sinh trực tiếp,
# xem src/merge.ts. Các biến dưới đây chỉ cần khi chạy import.ts (sau khi
# Phase 3 có Supabase project thật).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=